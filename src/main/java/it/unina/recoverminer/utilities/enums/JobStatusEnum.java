package it.unina.recoverminer.utilities.enums;

public enum JobStatusEnum {
    SUCCESS,
    FAILURE,
    ERROR,
    CURR_NO_COMPILE,
    CURR_NO_TEST_FAIL,
    PREV_TEST_FAILED,
    PREV_NO_COMPILE,
    CLONE_TIMEOUT,
    REF_NOT_FOUND,
    PROCESSED,
    SKIPPED,
    ALREADY_PROCESSED_SUCCESS,
    ALREADY_PROCESSED_FAILURE,
    ALREADY_PROCESSED_ERROR;
}
